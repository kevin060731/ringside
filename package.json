{
  "name": "ringside-fantasy-boxing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8765 --bind 127.0.0.1",
    "dev:lan": "python3 -m http.server 8765 --bind 0.0.0.0",
    "test": "node --test tests/*.test.js"
  }
}
